Note that this example uses ietype=199 - This means that the code will not look use your POTSDIR environment variable to find the potential files.
Instead, you must have a 'script.lmp' and potential files in the current working directory.
(For this example, these files are included in the inputs/ directory)... 

Copy the contents of this inputs/ directory into your current wording directory (which should be ~/spectad/examples/ag-lmp/ for this example)

Then, the code can be executed using the following commend (with the appropriate mpirun command for your system):

mpirun -np 4 ../../src/spectad.exe <trimer.input >trimer.lis &
